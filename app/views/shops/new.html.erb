<div class="shop-form-page">
  <div class="shop-form-header">
    <h1>新規店舗登録</h1>
    <p class="shop-form-lead">新しいラーメン店の情報を入力して、データベースに追加しましょう。</p>
  </div>

  <div class="shop-form-card">
    <% if @shop.errors.any? %>
      <div class="form-errors">
        <ul>
          <% @shop.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
   <%= form_with model: @shop, local: true,
  html: { class: "shop-form", multipart: true } do |f| %>
      <div class="form-group">
        <%= f.label :name, "店舗名", class: "form-label" %>
        <span class="req">必須</span>
        <%= f.text_field :name, class: "form-control", placeholder: "例：ラーメン二郎 三田本店" %>
      </div>

      <div class="form-group">
        <%= f.label :address, "住所", class: "form-label" %>
        <span class="req">必須</span>
        <%= f.text_field :address, class: "form-control", placeholder: "例：東京都港区三田2-16-4" %>
      </div>

      <div class="form-section-title">画像アップロード</div>

      <div class="upload-grid">
        <div class="upload-box">
          <div class="upload-title">建物の写真（1枚）</div>
          <%= f.file_field :building_photo, accept: "image/*", class: "file-input" %>
          <div class="upload-hint">PNG, JPG, GIF up to 10MB</div>
        </div>

        <div class="upload-box">
          <div class="upload-title">メニューの写真（最大5枚）</div>
          <%= f.file_field :menu_photos, multiple: true, accept: "image/*", class: "file-input" %>
          <div class="upload-hint">複数選択可（最大5枚）</div>
        </div>
      </div>

      <div class="form-section-title">詳細情報</div>

      <div class="form-group">
        <%= f.label :business_hours, "営業時間", class: "form-label" %>
        <%= f.text_field :business_hours, class: "form-control", placeholder: "例）11:00-15:00 / 18:00-22:00" %>
      </div>

      <div class="form-group">
        <%= f.label :closed_days, "定休日", class: "form-label" %>
        <%= f.text_field :closed_days, class: "form-control", placeholder: "例）水曜日" %>
      </div>

      <div class="form-group">
        <%= f.label :access, "アクセス", class: "form-label" %>
        <%= f.text_area :access, rows: 3, class: "form-control", placeholder: "例）○○駅から徒歩5分" %>
      </div>

      <div class="form-group">
        <%= f.label :notes, "備考", class: "form-label" %>
        <%= f.text_area :notes, rows: 4, class: "form-control" %>
      </div>

      <div class="form-actions">
        <%= link_to "キャンセル", shops_path, class: "btn-cancel" %>
        <%= f.submit "登録する", class: "btn-primary" %>
      </div>
    <% end %>
  </div>
</div>
