#!/usr/bin/env bash
sed -i 's/\r$//' bin/render-start
sed -i 's/\r$//' bin/docker-entrypoint
sed -i 's/\r$//' bin/rails

bundle exec rails db:prepare
bundle exec rails server -b 0.0.0.0 -p ${PORT:-10000}

